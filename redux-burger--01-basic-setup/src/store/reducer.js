import * as actionTypes from './actions';

// this is what our state should look like initially
// want to use the same state that we use in the BurgerBuilder
const initialState = {
    ingredients: null,
    totalPrice: 4
};

// get the state, and set it to initialState
const reducer = (state = initialState, action) => {

};

export default reducer;